{
  "name": "motion-sensor-raspberry-client",
  "private": true,
  "author": "Oron Nadiv <oron@nadiv.us> (https://github.com/OronNadiv/)",
  "homepage": "https://github.com/OronNadiv/motion-sensor-raspberry-client/",
  "bugs": "https://github.com/OronNadiv/motion-sensor-raspberry-client/issues",
  "repository": {
    "type": "git",
    "url": "https://github.com/OronNadiv/motion-sensor-raspberry-client.git"
  },
  "license": "AGPL-3.0",
  "engines": {
    "node": "^6",
    "npm": "^3"
  },
  "scripts": {
    "cover": "istanbul cover _mocha -- --recursive -t 10000",
    "lint": "eslint .",
    "start": "node src/index",
    "test": "mocha --recursive -t 10000 && npm run lint",
    "test-coveralls": "istanbul cover _mocha --report lcovonly -- --recursive -t 10000 -R spec && cat ./coverage/lcov.info | ./node_modules/coveralls/bin/coveralls.js && rm -rf ./coverage"
  },
  "dependencies": {
    "bluebird": "^3",
    "debug": "^2",
    "diehard": "^1",
    "jwt-generator": "^2.0.5",
    "http-as-promised": "^1",
    "moment": "^2.11.2",
    "pi-gpio": "^0.0.8",
    "raspberry-pi-led": "^1",
    "underscore": "^1"
  },
  "devDependencies": {
    "coveralls": "^2",
    "eslint": "^3",
    "eslint-config-standard": "^6.0.0",
    "eslint-plugin-promise": "^3.3.1",
    "eslint-plugin-standard": "^2",
    "istanbul": "^0.4",
    "mocha": "^3",
    "mockery": "^1",
    "nock": "^8",
    "should": "^11"
  }
}
